/**
 * Public API of the home feature — the landing city picker. `app/page.tsx`
 * renders `HomeView`; the `city-picker/` internals stay behind this barrel.
 */
export { HomeView } from "./home-view";
