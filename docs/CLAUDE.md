@AGENTS.md

## Claude Code

Keep this file Claude-specific. Shared project rules belong in `AGENTS.md` or
the human-facing docs under `docs/`.

If this file is copied to the repository root later, keep the import above so
Claude reads the shared agent guidance without duplicating it.
