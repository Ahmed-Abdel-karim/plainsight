# Map Machine Transition Gating

> **SUPERSEDED - historical background only.** The current map machine is a
> parallel XState machine with `lifecycle` and `interaction` regions, and the
> root machine uses `settled <-> switching`. The current implementation reference
> is [`../_docs/architecture.md`](../_docs/architecture.md).

This file previously captured design sketches for nested map suppression and
earlier navigation experiments. Those details no longer describe shipped code,
so the full stale sketch was removed to avoid conflicting architecture guidance.
