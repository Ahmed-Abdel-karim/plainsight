# Scene Navigation Architecture

> **SUPERSEDED - do not use as current guidance.** The current scene runtime,
> provider lifetime, root `settled <-> switching` states, navigation path
> tracker, map parallel regions, worker transport, data tiers, and URL sync model
> are documented in [`../_docs/architecture.md`](../_docs/architecture.md).

This old `docs/` file used to be the navigation reference while the actor system
was being migrated. It is intentionally reduced to a pointer so it cannot keep
contradicting the implemented system.
