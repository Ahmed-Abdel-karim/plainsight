/**
 * Provenance footer — the trust anchor line for the market panel.
 * Pure presentational, static copy.
 */
export function DataProvenance() {
  return (
    <footer className="type-caption-mono text-balance text-muted-foreground">
      Every figure traces to a single dated public snapshot. No estimates, no
      live data.
    </footer>
  );
}
