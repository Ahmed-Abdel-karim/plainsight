/**
 * Map-region test data. The framing + the browse points/boundaries fixtures are the
 * shared trimmed-real handles; the MSW default handlers already serve the points and
 * boundaries tiers, so the layer sources fill without per-test wiring.
 */
export {
  boundariesFixture,
  browsePointsFixture,
  makeMapCityPayload,
} from "@/test/fixtures/browse";
